name: flutter_dependency_doctor

description: An intelligent dependency health and optimization tool for Dart and Flutter projects.

version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.7.0
  yaml: ^3.1.3
  http: ^1.5.0
  pub_semver: ^2.2.0
dev_dependencies:
  test: ^1.26.0
  lints: ^6.0.0

executables:
  flutter_dep: flutter_dep
